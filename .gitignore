# === HarmonyOS (root client + elderly/) ===
/node_modules
/oh_modules
/local.properties
/.idea
**/build
/.hvigor
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer

# === Generic build artifacts ===
**/node_modules/
**/oh_modules/
**/build/
**/.hvigor/
**/.idea/
**/.appanalyzer/
**/.test/
**/__pycache__/

*.tsbuildinfo
*.tsbuildinfo.linter
*.py[cod]
*.log

# === Go backend (server/) ===
# Compiled binaries
server/kangxiaoban-server
server/kangxiaoban-server-linux
server/kangxiaoban-service/
server/server.exe
server/server-check.exe
server/kxb-deploy-linux-amd64
server/bin/

# Runtime data — never commit database or identity documents
server/data/
server/uploads/
server/*.db
server/tmp/

# Environment / secrets
server/.env

# Stray HarmonyOS artifacts inside server/
server/.hvigor/
server/.codex/

# === Local verification artifacts ===
tmp/
screenshots/

# === Agent / tool config ===
.claude/settings.local.json

# Agent-local connection credentials. Tracked AGENTS.md may reference files in
# this directory, but their contents must never be committed or printed.
.codex/private/
.codex/private/*.json

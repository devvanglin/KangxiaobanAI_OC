# 2026-09-06 工作日志

## 全项目理解梳理（只读扫描，无代码改动）

对 D:\Coding\KangxiaobanAI_OC 做了一次完整结构盘点，结论如下（供后续会话直接复用）：

### 仓库结构（顶层 825 项，绝大多数是设备调试截图/布局 xml，不是代码）
真正的一等公民只有 4 个：
1. `KangxiaobanAI/` —— 主产品，HarmonyOS NEXT 原生（ArkTS V2），bundleName `com.gxoc.kxbai`，模块 `kanxiaoban`，srcPath `products/entry`，targetSdk 26.0.0 / compatible 6.1.1(24)，已配置本地签名（build-profile.json5 有 signingConfigs 硬编码口令，属敏感内容不可外传）。63 个 .ets 共 36725 行，入口 `pages/LoginPage` → `pages/MainPage`。
2. `kangxiaoban-service/` —— Go 后端（Gin+GORM，module kangxiaoban-service，go 1.26.5），124 个 .go 共 22931 行，含 20+ 测试文件（admission 系列为主）。internal 下：auth/config/database/handler/healthrisk/iot/middleware/model/operationpolicy/pkg/repository/router/security/service/system/ws。
3. `KanxiaobanDS/` —— 驾驶舱鸿蒙端，读 `/dashboard/cockpit`。
4. `hermes/` —— 完全独立的 Hermes Studio 鸿蒙移植项目（上游 EKKOLearnAI/hermes-studio @ d9ea9f4，v0.6.42），有自己 AGENTS.md + HERMES_PORTING_MEMORY.md，与康小伴无业务关系。

其余：nurse-dashboard/、showcase/（纯 HTML 展示页）；MusicHome 等 13 个华为官方 sample（只读参考）；eldercare-projects-100-20260820/ + elderly-care-repos/（100 个养老开源项目参考资料库）；kxb-redesign.design/（设计稿）；MTQQ/（60G 毫米波雷达资料）。

### 后端要点
- 路由全部在 internal/router/router.go，`/api/v1` 前缀，JWT + RBAC（middleware.RequirePermission）。权限码：admin:all / dash:read / elder:read|write / task:read|write / health:read|write / care:review / plan:manage / alert:read|handle / admission:read|write。
- 公开端点仅 4 个：POST /auth/login、GET /public/dashboard、GET /ws、GET /iot/preview/:id/:token/:file。
- 多租户：model.TenantContextKey 注入 ctx，db.WithContext 作用域隔离；middleware.Audit 写审计。
- DB 双驱动：sqlite 开发 / mysql 生产，KXB_DB_DRIVER 切换。业务常量（护理等级费用、体征阈值、床位使用率、AI 提示词、评估题库）全部入库按租户管理，客户端不存业务常量。
- 设备接入：internal/iot/mqtt.go 订阅 `/Radar60SP/+/sys/property/post`（睡眠雷达→product=breath_radar）与 `/Radar60FL/+/sys/property/post`（跌倒→fall_radar），默认 Broker `tcp://192.168.100.110:1883`。ingest 归一化后写 SignalRecord；若设备已绑定 ElderID 则投影成 HealthRecord（heartRateValue/breathValue）；告警规则：fallStatus∈{1,2,3}→emergency；呼吸/心率按 HealthThreshold 走 healthrisk.EvaluateMetric。有离线扫描（30s）+ 告警升级扫描 + 冷却去重。
- 摄像头：RTSP→HLS（ffmpeg），签名令牌放路径里。
- AI：可插拔 provider（local/http），会话与消息持久化，有 Dify RAG 代理（/admin/ai/rag/proxy/*path）与用量统计。

### 前端要点
- 角色三档：admin / doctor / caregiver（后端为准，LoginPage.resolveRole 优先级 admin>doctor>caregiver）。手机端四 tab（首页/长者/消息/我的）；宽屏（≥720vp 断点，≥1180vp 全工作区）走 Wide* 组件：WideCaregiverWorkspace / WideDoctorResidentPage / WideDoctorAdmissionForm / WideAdminWorkspace / WideModelManagement 等。
- 数据层：ApiConfig 硬编码 `http://10.10.1.12/api/v1` 与 `ws://10.10.1.12/api/v1/ws`（部署目标机）。ApiClient 有 8s/10s/12s 三级超时 + GET/login 单次重试。BusinessStore 是 V2 @Trace 单例聚合（elders/rooms/beds/tasks/devices/alerts/healthRecords/messages…）。WsClient 收实时事件。
- 代码量集中：TabPageView 3599 行、WideHomePage 3535、WideDoctorAiPage 2415、AiChatPage 2414、WideDoctorAdmissionForm 2286。

### Git
- 远端 origin https://github.com/devvanglin/KangxiaobanAI_OC.git，当前分支 shenyi_dev，HEAD 2f134941（kb 页面 HdsNavDestination 沉浸式对齐）。工作区 806 项 dirty（几乎全是截图/布局 dump）。

### 环境备忘
- 后端部署机 10.10.1.12（SSH 用户 api，凭据在 .codex/private/backend-ssh.json，只读不打印）。

---

## 后端服务器实际登录侦察（paramiko SSH，全程只读，第二轮会话）

- 登录成功：api@10.10.1.12（Ubuntu 26.04.1，主机名 prod-race202609-api，8C Xeon Gold 6430 / 7.2G 内存 / 502G 盘用 5%，已运行 4 天，负载极低）。
- **全部服务 Docker 化**（api 需 sudo 才能操作 docker；sudo -S 需逐命令经 stdin 喂密码）：
  - `kangxiaoban-service`：自建镜像，80→8080，restart=unless-stopped，挂载 `./data:/data`（SQLite kangxiaoban.db + WAL 活跃写入）。验证 POST /api/v1/auth/login 返回业务 JSON（400 参数错误）→ 服务存活。
  - Dify 1.17.0 全家桶（compose 前缀 docker-）：api/web/worker/websocket/beat/plugin_daemon(5003)/sandbox/agent_backend(5050)/weaviate 1.27.0/postgres 15/redis 6/nginx(443、8080→80)。
  - `new-api`：calciumion/new-api LLM 网关，3030→3000。
- 部署目录 `/opt/kangxiaoban/kangxiaoban-service`：Go 1.26.5 源码（cmd/internal/go.mod/Dockerfile/docker-compose.yml/.env）+ data/ + 十余个 kangxiaoban-server.bak-* 二进制备份；**不是 git 仓库**。/opt/kangxiaoban/showcase 静态展示页；/opt/dify/src 为 Dify 源码。
- .env 键（值未读）：KXB_SERVER_PORT/DB_DRIVER/SQLITE_PATH/JWT_SECRET/JWT_EXPIRE/MQTT_ENABLE/MQTT_URL/MQTT_USERNAME/MQTT_PASSWORD/AI_ENABLED/AI_PROVIDER/SEED_BUSINESS/DEPLOY_MODE/APP_VERSION。compose 默认 MQTT → `tcp://10.10.1.14:1883`（注意与本地代码默认 192.168.100.110 不同，以服务器 .env 为准）。
- /home/api 候选新二进制：kangxiaoban-server.new（41.7MB ELF Go 静态链接，9/5）、kangxiaoban-server.geom（28.9MB）、kxb-server-linux-amd64；rag_diag.py 诊断脚本。
- 本机无 sshpass/plink；可用方案 = venv paramiko（C:/Users/Shenyi/.workbuddy/binaries/python/envs/default/Scripts/python.exe，paramiko 5.0.0），临时脚本已清理。

---

## 登录"无法连接服务器"排查与修复（第三轮会话）

- **根因**：工作区有一处未提交的 `ApiConfig.ets` 本地改动，把后端地址从已提交的正确值 `10.10.1.12`（HTTP 80，提交 30ab71ff "chore: switch backend to port 80"）改回了 `192.168.100.110:8080`。实测 192.168.100.110:8080 返回 502（那台机器有网关但没跑康小伴后端），10.10.1.12:80 正常（登录接口 10ms 返回业务 JSON）。
- **已修复**：`KangxiaobanAI/products/entry/src/main/ets/network/ApiConfig.ets` 的 API_BASE/WS_URL 均已改回 `http(s)://10.10.1.12/...`（相对 HEAD 仅剩注释行尾差异）。
- **重新构建受阻**：hvigor 命令行构建（DEVECO_SDK_HOME + JBR 已配好），CompileArkTS 编译通过（仅 ai-history-search 组件 ID 重复的 WARN），但 `@PackageHap` 阶段 hvigor 调 `wmic.exe` 被 WorkBuddy 安全中心程序黑名单拦截（连带报 spawn java ENOENT），沙箱内明确提示不可绕过。未完成打包安装。
- 环境事实：DevEco hvigorw = `C:/Program Files/Huawei/DevEco Studio/tools/hvigor/bin/hvigorw.bat`；JBR = DevEco Studio/jbr；KangxiaobanAI/build-profile.json5 **有**本地 debug signingConfigs（与 AGENTS.md "无 signingConfigs" 的描述漂移，以实际文件为准）；hdc = sdk/default/openharmony/toolchains/hdc.exe，模拟器 127.0.0.1:5555。
- 恢复路径：用户把 wmic.exe 移出黑名单后我重跑构建+hdc install；或用户在 DevEco Studio 直接 Run（源码可编译）。

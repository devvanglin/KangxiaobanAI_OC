# 智能养老 Docker 部署晨间核查报告

- 核查时间：2026-08-21 08:00（自动任务）
- 服务器：192.168.100.104:22，用户 pve
- 机器规格：4 核 / 8 GB 内存

## 一句话结论

本地那份 `deploy_report`（87 个项目、端口 8001–8087）**一个都没部署成功**，全是构建失败；服务器上实际跑的是另一批 `ecXXX` 容器。核查时只有 4 个在跑、11 个被内存挤掉（OOM 杀掉），我已把它们全部拉起，**现在 15 个容器在线、6 个项目栈在运行**，其中 5 个网页可直接打开试用。

## 1. 本地报告 vs 服务器真实情况（重要）

| 来源 | 内容 | 真相 |
|---|---|---|
| deploy_report.csv / .json（87 项） | 全部 error / build_fail | 这批项目构建阶段就失败（多为 `[Errno 13] Permission denied`、Maven 无 jar、前端无 dist），**从未生成容器**，docker 里查无此批 |
| 服务器 docker（ecXXX 批） | 16 个镜像、分批构建 | 真正部署的是这批；初始仅 4 个在线 |

> 本地报告是「构建结果」，不是「运行状态」。两者不是同一批项目，别混为一谈。

## 2. 当前实际运行容器（docker ps，15 个）

| 容器 | 状态 | 对外端口 | 类型 |
|---|---|---|---|
| ec002-web | Up | **18002** | 网页前端 ✅ |
| ec002-api | Up | 内网 | 后端 API |
| ec002-db | Up | 5432 内网 | 数据库 |
| ec006-web | Up | **18006** | 网页前端 ✅ |
| ec006-api | Up | 内网 | 后端 API |
| ec008-web | Up | **18008** | 网页前端 ✅ |
| ec008-api | Up | 内网 | 后端 API |
| ec042-web | Up | **18042** | 网页前端 ✅ |
| ec042-api | Up | 内网 | 后端 API |
| ec043-web | Up | **18043** | 网页前端 ✅ |
| ec043-api | Up | 内网 | 后端 API |
| ec073-api | Up | 18073 | API（根路径返回 500，见下） |
| ec100-redis | Up 15h | 内网 | 缓存 |
| ec100-mysql | Up 16h healthy | 3306 内网 | 数据库 |
| elderly-mysql | Up 29h | 13306 | 数据库 |

- 内存占用：2.6 GB / 7.7 GB，**可用 4.8 GB，健康无 OOM 压力**。
- web 前端的 api/db 通过容器内部网络互通，从浏览器只访问 web 端口即可。

## 3. 可直接打开试用的 5 个最稳项目（HTTP 200 已验证）

> 局域网内浏览器直接打开，无需登录即可看界面（登录账号见第 5 点）。

1. http://192.168.100.104:18002  （ec002 养老项目栈）
2. http://192.168.100.104:18006  （ec006 养老项目栈）
3. http://192.168.100.104:18008  （ec008 养老项目栈）
4. http://192.168.100.104:18042  （ec042 养老项目栈）
5. http://192.168.100.104:18043  （ec043 养老项目栈）

（第 6 个 ec073-api 端口 18073 容器在跑，但根路径返回 **500**，应用层报错，需查 `docker logs ec073-api` 定位，暂不列入「最稳」。）

## 4. 失败 / 未运行项目及原因

- **本地报告 87 项（端口 8001–8087）全部未部署**：构建阶段失败——
  - 大量 `[Errno 13] Permission denied`（Dockerfile / 脚本权限问题）；
  - 一批 Spring Boot 项目 `Maven 构建无 jar`（缺 pom 或构建命令不对）；
  - 一批前端项目 `构建无 dist`（缺 build 脚本）。
- **服务器 ecXXX 批里被 OOM 杀掉的 11 个**：已在本轮核查中 `docker start` 拉起，目前全部 Up。

## 5. 登录信息

- 本地报告 `login` 列为空，未记录明文凭据。
- 这批多为若依 / Spring Boot 类后台，常见默认账号 `admin / 123456` 或 `admin / admin123`，以各自登录页提示为准。
- 数据库（ec100-mysql 3306、elderly-mysql 13306、ec002-db 5432）为后端依赖，无需单独登录。

## 6. 内存限制说明（4 核 / 8 GB）

- 当前 15 个容器**已接近上限**（约保活「十余个」）。再加会触发 OOM 把容器杀掉（退出码 137/143）。
- 其余已构建完成的镜像随时可启动：
  `echo 123456 | sudo -S docker start ecXXX_容器名`
- 建议：只保活真正要演示的 5~6 个 web 栈，其余需要时再 start，别一次性全拉。

## 7. 本轮已执行动作（供回顾）

- SSH 连服务器，执行 `docker ps` / `docker ps -a` 确认真实状态；
- `docker start` 拉起全部 11 个 OOM 退出的 ecXXX 容器；
- `curl` 验证 6 个 web 端口，5 个返回 200、1 个（18073）返回 500。
